apiVersion: 1

providers:
  - name: MedTrack
    orgId: 1
    folder: MedTrack
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
